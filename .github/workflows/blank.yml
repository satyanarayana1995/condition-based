name: Deploy WAR to Azure Web Apps

on:
  workflow_dispatch:
    inputs:
      
      environment:
        description: 'Environment to deploy against (staging or prod)'
        required: true
        type: choice
        options:
          - staging
          - prod

jobs:
  deploy-staging:
    if: inputs.deployment_stage == 'staging'  # ✅ Runs only if stage-1 is selected
    environment: staging
    runs-on: ubuntu-latest

    steps:
    - name: Echo message for Staging
      run: echo "Deploying to Staging Environment

  deploy-prod:
    if: inputs.deployment_stage == 'prod'  # ✅ Runs only if stage-1 is selected
    environment: prod
    runs-on: ubuntu-latest

    steps:
    - name: Echo message for Staging
      run: echo "Deploying to prod Environment
